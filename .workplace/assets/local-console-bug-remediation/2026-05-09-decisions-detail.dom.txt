OpenClaw

GUARDIAN CONSOLE

工作台
概览
问答记录
审计链路
审计日志
工具调用
多轮链路
审批闭环
审批管理
决策观测
放行记录
治理
策略配置
运行资产
检测报告
会话
Token 统计
Skill 供应链
收起侧边栏
决策观测
|
DECISION CONTROL PLANE
混合
决策观测

DECISION CONTROL PLANE

待复核

1

已阻断

0

需要审批

1
风险等级
全部级别
裁决阶段
全部阶段
执行动作
全部动作
关键词
应用筛选
重置条件
裁决记录说明
回答“系统为什么这么判”：列表保留裁决、决策理由、风险、动作、审批和处置结果；告警类裁决需要查看详情证据时进入详情。
裁决记录
裁决	决策理由	风险	动作	审批	处置结果	操作

assistant_output-1778321149637
输出

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

tool_call-1778321139305
工具

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

input-1778321133668
输入

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

tool_call-1778321020123
工具

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

tool_call-1778321020115
工具

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

input-1778321008622
输入

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

tool_call-1778320921147
工具

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

tool_call-1778320908663
工具

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

input-1778320886634
输入

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

assistant_output-1778320857324
输出

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

input-1778320818832
输入

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

assistant_output-1778320782642
输出

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

tool_call-1778320699529
工具

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

tool_call-1778320699517
工具

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

input-1778320689507
输入

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

assistant_output-1778291589428
输出

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

input-1778291578815
输入

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

assistant_output-1778287061022
输出

这次被判为 L3 高危，因为命中了 中文规避意图（evasive_intent_cn）。处置结果是需要人工审批，不是直接放行；审批通过后才可能产生后续放行记录。L4 是硬拒绝，不能审批放行。
	L3 高危	审批
需要审批
	未阻断	详情

input-1778287022631
输入

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

assistant_output-1778249430473
输出

这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能审批放行。
	L0 基础	放行
无需审批
	未阻断	详情

第 1 页 / 共 19 页，显示 20 条结果，共 375 条

1
2
3
4
5
•••
19
20 条/页
裁决详情

输出阶段 · 语义风险信号（semantic） · L0 基础 · 放行 · 目标未记录 · 这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能...

×

裁决概览

输出阶段 · 语义风险信号（semantic） · L0 基础 · 放行 · 目标未记录 · 这次被判为 L0 基础，因为命中了 语义风险信号（semantic）。处置结果是允许继续，但仍保留这次判断依据，方便之后回看为什么没有拦截。L4 是硬拒绝，不能...

裁决阶段
输出
风险等级
L0 基础
执行动作
放行
处置结果
未阻断
审批状态
无需审批
基础信息

裁决阶段、审计策略、审批状态和降级上下文。

裁决 ID
assistant_output-1778321149637
阶段
输出
风险等级
L0 基础
动作
放行
审计策略
允许
执行动作
放行
审批状态
无需审批
处置结果
未阻断
降级原因
无降级
策略版本
7
判断依据

命中模块、规则和分数变化，用来复核裁决来源。

获胜仲裁器
semantic_intent
命中模块
语义风险信号（semantic）
命中规则
semantic.ordinary_business
证据状态
None
评分明细
semantic.ordinary_business +0